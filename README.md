How to run the Orleans program

- In `Streamclient.cs`, set `useTestData` as true, and set `useKafka` as false
- Run Orleans server `dotnet run --project .\Server`
- Run Orleans client `dotnet run --project .\Client`
